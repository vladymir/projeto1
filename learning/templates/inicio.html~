{% extends "base.html" %}

{% block title %}KickStart | 
        <div id="nav">
            <a href="/">home</a> |
            {% if user.is_authenticated  %}
                Bem vindo {{ user.username }}
                (<a href="/logout">logout</a>)
                {% else  %}
                <a href="/login/">login</a>
                <a href="/register/">cadastrar</a>
                {% endif %}
            </div>
{% endblock %}
{% block content %}
   <h1>Bem vindo ao kickstart</h1>
    {% if user.username %}
        <p>Bem vindo {{ user.username }}!
            Aqui voce podera criar seu projeto!</p>
    {% else %}
        <p>Bem vindo, voce precisa fazer o login para acessar nosso servico.</p>
    {% endif %}
{% endblock %}
</body>
</html>
